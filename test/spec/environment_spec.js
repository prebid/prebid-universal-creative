import { expect } from 'chai';
import { newEnvironment } from 'src/environment';

describe('environment module', function() {
  
  it('should return env object with proper public api', function() {
    const env = newEnvironment(win);
    
  });
  
});

